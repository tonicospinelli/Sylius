{% if is_granted('ROLE_USER') %}
<ul class="links">
    <li><a href="{{ path('sylius_account_homepage') }}">My account</a></li>
    <li><a href="{{ path('fos_user_security_logout') }}">Log out</a></li>
    {% if is_granted('ROLE_SYLIUS_ADMIN') %}
    <li><a href="{{ path('sylius_backend_index') }}">Administration</a></li>
    {% endif %}
</ul>
{% else %}
<a href="{{ path('fos_user_security_login') }}">Sign in</a> or <a href="{{ path('fos_user_registration_register') }}">Create account</a>
{% endif %}
